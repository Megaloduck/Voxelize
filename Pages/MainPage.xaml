<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:Voxelize.PageModels"             
             xmlns:models="clr-namespace:Voxelize.Models"
             xmlns:controls="clr-namespace:Voxelize.Pages.Controls"
             xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="Voxelize.Pages.MainPage"
             x:Name="OverviewPage"
             Title="{Binding Today}">

    <VerticalStackLayout Padding="20" Spacing="10">

        <Button Text="Load Image"
                Command="{Binding LoadImageCommand}" />

        <Label Text="Pixel Size"/>
        <Slider Minimum="2"
                Maximum="32"
                Value="{Binding PixelSize}" />

        <Label Text="Color Depth"/>
        <Slider Minimum="2"
                Maximum="64"
                Value="{Binding ColorDepth}" />

        <Button Text="Convert"
                Command="{Binding ApplyPixelArtCommand}" />

        <Image HeightRequest="300"
               Source="{Binding PreviewImage}" />

    </VerticalStackLayout>

</ContentPage>
